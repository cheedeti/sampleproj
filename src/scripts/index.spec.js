/* eslint-env mocha */
import { assert } from 'chai';
import index from './index';

describe('index', () => {
    it('place holder test', () => {
        index('placeholder');
        assert.ok(true, 'place holder test');
    });
});
